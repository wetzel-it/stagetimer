# ============================================================
# StageTimer - Umgebungsvariablen (OPTIONAL)
# ============================================================
# Diese Datei ist OPTIONAL. Die App funktioniert ohne Konfiguration.
# Nur kopieren wenn du etwas anpassen moechtest:
#   cp .env.example .env
# ============================================================

# Flask Secret Key (OPTIONAL)
# Wird automatisch generiert und in .secret_key gespeichert.
# Nur setzen wenn du einen eigenen Key verwenden moechtest.
# SECRET_KEY=dein_geheimer_key

# Flask Environment
# - production: Optimiert fuer Produktion, weniger Logs (Standard)
# - development: Debug-Modus, Auto-Reload bei Code-Aenderungen
FLASK_ENV=production

# Log Level
# - ERROR: Nur Fehler (Standard, empfohlen fuer Produktion)
# - WARNING: Fehler + Warnungen
# - INFO: Fehler + Warnungen + Info-Meldungen
# - DEBUG: Alles (nur fuer Entwicklung)
LOG_LEVEL=ERROR

# Timezone (fuer korrekte Zeitanzeige)
TZ=Europe/Berlin

# Port (Standard: 5000)
# PORT=5000
