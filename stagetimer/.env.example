# Flask Secret Key für Session-Management
# Generiere einen sicheren Schlüssel mit: python -c "import os; print(os.urandom(24).hex())"
SECRET_KEY=your_secret_key_here

# Flask Environment (development oder production)
FLASK_ENV=production

# Timezone
TZ=Europe/Berlin

# Optional: Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional: Port (Standard: 5000)
# PORT=5000
